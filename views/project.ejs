<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= project.title %></title>
  <link href="/css/main.css" rel="stylesheet" />
</head>
<body>
  <%- include('partials/navbar', { page: '' }) %>

  <div class="container mx-auto my-6">
    <div class="hero bg-base-200 p-6 rounded-lg mb-6">
      <div class="hero-content text-center">
        <div class="max-w-xl">
          <h1 class="text-5xl font-bold"><%= project.title %></h1>
          <p class="py-6">Below, you will find detailed information about the project: <%= project.title %></p>
        </div>
      </div>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <div>
        <img class="rounded-lg" src="<%= project.feature_img_url %>" alt="Project Image" />
      </div>
      <div>
        <p class="mb-4"><%= project.intro_short %></p>
        <p class="mb-4"><strong>Impact:</strong> <%= project.impact %></p>
        <p class="mb-4">
          <a href="<%= project.original_source_url %>" class="btn" target="_blank">View Original Source</a>
        </p>
        <div class="mt-4 text-sm italic" id="quote"></div>
        <script>
          document.addEventListener("DOMContentLoaded", async () => {
            const res = await fetch("https://dummyjson.com/quotes/random");
            const data = await res.json();
            document.getElementById("quote").innerText = `"${data.quote}" — ${data.author}`;
          });
        </script>
        <a href="#" onclick="history.back(); return false;" class="btn btn-outline mt-4">Go Back</a>
      </div>
    </div>
  </div>
</body>
</html>
